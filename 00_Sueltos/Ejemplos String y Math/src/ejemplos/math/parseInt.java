/*
parseInt es una función integrada de JavaScript que toma 
una cadena de caracteres como argumento y la analiza como 
un número entero. Retorna una representación entera de la 
cadena o NaN (No es un número) si la cadena no puede ser 
analizada como un entero.
 
La sintaxis de parseInt es la siguiente:

parseInt(string, radix)

Donde:

string: la cadena de caracteres que se va a analizar 
y convertir en un número entero.

radix: la base numérica en la que está representada la cadena. 
Este argumento es opcional y su valor por defecto es 10. 
Es decir, si no se especifica la base numérica, se asume que 
la cadena está representada en base 10.

Por ejemplo, para analizar la cadena "10" y convertirla en 
un número entero, se puede usar la siguiente expresión:

parseInt("10")

El resultado será el número entero 10.

También se puede especificar la base numérica de la cadena 
usando el segundo argumento de parseInt. 

Por ejemplo, para analizar la cadena "1010" como un número binario, 
se puede usar la siguiente expresión:

parseInt("1010", 2)

El resultado será el número entero 10 en base 10, 
que es el valor decimal equivalente de la representación binaria 
de la cadena "1010".

*/
package ejemplos.math;

public class parseInt {
    public static void main(String[] args) {
        // TODO code application logic here
    }
    
}
